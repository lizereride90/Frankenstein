"""Place command cogs in this package.

To add a new cog:
- create a Python file (e.g. moderation.py)
- define a subclass of commands.Cog
- expose a `setup(bot)` async function that calls `await bot.add_cog(...)`
"""
